import {COLORS} from '@constants/colorConstants';
import {StyleSheet} from 'react-native';

export const navigationHeader = StyleSheet.create({
  headerStyle: {
    backgroundColor: COLORS.GRAY,
    elevation: 0,
    shadowOpacity: 0,
  },
});
